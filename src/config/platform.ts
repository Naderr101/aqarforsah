// Platform-level configuration. Values will later come from server settings.
export const platformConfig = {
  /** Buyer platform fee as a fraction of the transaction value (1.25%). */
  exitPlatformFeeRate: 0.0125,
  currencyLabel: "جنيه",
} as const;
